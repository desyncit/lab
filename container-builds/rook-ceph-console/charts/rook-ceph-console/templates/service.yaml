apiVersion: v1
kind: Service
metadata:
  {{- if not .Values.certificateSecretName }}
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: {{ template "rook-ceph-obc-plugin.certificateSecret" . }}
  {{- end }}
  name: {{ template "rook-ceph-obc-plugin.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rook-ceph-obc-plugin.labels" . | nindent 4 }}
spec:
  ports:
    - name: {{ .Values.plugin.port }}-tcp
      protocol: TCP
      port: {{ .Values.plugin.port }}
      targetPort: {{ .Values.plugin.port }}
  selector:
    {{- include "rook-ceph-obc-plugin.selectorLabels" . | nindent 4 }}
  type: ClusterIP
  sessionAffinity: None
