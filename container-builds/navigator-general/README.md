### General Purpose ansible-navigator build

Builds the a general ansible-navigator image, then pushes to container 
registry -> [jherron/ee-centos](https://quay.io/repository/jherron/ee-centos?tab=history)

```bash
$ >> make 
```
