### Custom ansible-navigator build

Builds the ansible-navigator image, then pushes to container registry -> [jherron/ee-centos](https://quay.io/repository/jherron/ee-centos?tab=history)

```bash
$ >> make 
```
